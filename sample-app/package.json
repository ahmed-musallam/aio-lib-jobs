{
  "name": "aio-lib-jobs-sample",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "aio-lib-jobs": "file:..",
    "@adobe/aio-lib-state": "^5.3.1",
    "openwhisk": "^3.21.9",
    "hono": "^4.13.2",
    "hono-openwhisk-adapter": "^2.1.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "jest": "^29",
    "ts-loader": "^9.5.1",
    "typescript": "^5.5.4",
    "eslint": "^8",
    "eslint-plugin-jest": "^27.2.3"
  },
  "scripts": {
    "test": "jest --passWithNoTests ./test",
    "e2e": "jest --collectCoverage=false --testRegex ./e2e",
    "lint": "eslint --no-error-on-unmatched-pattern test src actions",
    "lint:fix": "npm run lint -- --fix"
  },
  "engines": {
    "node": ">=18"
  }
}
